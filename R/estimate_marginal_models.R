estimate_marginal_models <- function(
  data,
  n_all_obs, n_marg_train, n_marg_refit,
  all_asset_names,
  marginal_settings,
  trace
) {
  rugarch
}

estimate_marginal_models(data_margmod_input,
                         1000, 750, 50,
                         c("AAPL", "GOOG", "AMZN"),
                         NULL,
                         TRUE)
